#include<stdio.h>
 int main()
{
	char ch[322];
	fgets(ch,sizeof(ch),stdin);    //应该是十个字节的话，fgets输入的话第十个是空格，意思就只能输九个字节的东西。 
	printf("%s",ch);
	return 0;
}

//用  fputs  输出，下面的应该是规范样式，它没自带换行
#include<stdio.h>
 int main()
{
	char ch[322]="baiuyinshj";
	fputs(ch,stdout);
	return 0;
}
