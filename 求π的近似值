/* 用下列公式求出π的近似值，直到最后一项绝对值小于10^-6为止 */
/* π/4≈1-1/3+1/5-1/7+... */
#include<stdio.h>
#include<stdio.h>
main()
{
    int s;
    float n,t,pi;
    t=1,pi=0,n=1.0,s=1;
    while(fabs(t)>=1e-6)
      {
        pi=pi+t;
        n=n+2;
        s=-s;
        t=s/n;
        }       
     pi=pi*4;
     printf("pi=%10.6f/n",pi);
}
