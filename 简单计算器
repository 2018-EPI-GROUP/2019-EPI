#include<stdio.h>
 int main()
 {int a,b,c;
  char op;
  scanf("%d%c%d",&a,&op,&c);
   if('+'==op)
     {c=a+b;}
   else if('-'==op)
     {c=a-b;}
   else if('*'==op)
     {c=a*b;}
   else if('/'==op)
     {c=a/b;}
   else {printf("无效输入/n");}
  printf("%lf",c);
  return 0;
}
