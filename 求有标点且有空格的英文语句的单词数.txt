#include <stdio.h>
#include <string.h>
#include <ctype.h>

    int main(void){
    int c = o,n,i;
    char a[200];
    
    printf("请输入一段有空格并且有标点的英文句子:\n");
    gets(a);
    n = strlen(a);
 
    i = 0;
    while(i <= n-1){
    if(isalnum(a[i])&&!isalnum(a[i+i]))
        c++;
    i++;
    }
    printf("这句话中包含的单词有%d个\n",c);

    return 0;
}   