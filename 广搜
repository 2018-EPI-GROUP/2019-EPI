#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
int F[3000][3000];
bool V[3000];
int qu[3000];
int w=1,n,sum=0,d=1;
void gs(int s)
{
  if(sum==n)return;
  printf("%d ",s);
  sum++;
  for(int i=1;i<=n;i++)
    if(F[s][i]==1 && V[i]==false){qu[w++]=i;V[i]=true;}
  gs(qu[d++]);
  return ;
}
int main()
{
  memset(F,666,sizeof(F)); 
  memset(V,false,sizeof(V));
  int m;
  scanf("%d%d",&n,&m);
  for(int i=1;i<=m;i++)
  {
    int x,y;
    scanf("%d%d",&x,&y);
    F[x][y]=1;
  }
  V[1]=true;
  gs(1);
  return 0;
}


 
