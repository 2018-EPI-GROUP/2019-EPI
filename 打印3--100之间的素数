#include <stdio.h>
main()
{
	int n, i, m = 0;
	for (n = 3;n <= 100;n++)
	{
		for (i = 2; i <= n;i++)
			if (n % i == 0) break;
		if (i >= n)
		{
			printf("%d ", n);
			m++;
			if (m % 10 == 0) printf("\n");
		}
	}
}
