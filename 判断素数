#include<stdio.h>
main()
{
	int m;
	int i;
	int flag;
	printf("m");
	scanf("%d",&m);
	flag=1;
	for(i=2;i<m;i++)
	  if(m%i==0 )
	  {
	  	flag=0;
	  	i=m;
	  	
	  }
	  if(flag==1)
	  printf("%d\n;m");
	  else
	  printf("%d\n;m");
}

